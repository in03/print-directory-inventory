from distutils.core import setup

setup(
    name = 'print-directory-inventory',
    packages = ['print-directory-inventory'],
    version = '1',  # Ideally should be same as your GitHub release tag varsion
    description = 'Recursively lists all files and their last modified dates into a .csv file',
    author = 'Caleb Trevatt',
    author_email = 'pebblefarmer@outlook.com',
    url = 'https://github.com/in03/print-directory-inventory/archive/1.tar.gz',
    download_url = 'download link you saved',
    keywords = ['directory', 'print', 'walk', 'csv'],
    classifiers = [],
)